http:
  routers:
    auth-service-router:
      rule: "PathPrefix(`/auth`)"
      service: auth-service
      entryPoints:
        - web

  services:
    auth-service:
      loadBalancer:
        servers:
          - url: "http://auth-service:3000"
